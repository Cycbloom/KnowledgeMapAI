# 分析跳过的测试用例

## 目标

分析 44 个被跳过的测试用例，确定哪些可以启用。

## 跳过测试分类

### 1. 登录模块 (login.spec.ts) - 5 个跳过

| 测试名称 | 跳过原因 | 建议 |
|---------|---------|------|
| `高并发登录场景（5 个用户）应正常处理` | 性能测试 | ✅ 可以启用 |
| `并发登录时服务器应正确处理请求顺序` | 性能测试 | ✅ 可以启用 |
| `清除 Cookie 后应重定向到登录页` | 会话管理 | ✅ 可以启用 |
| `会话 Cookie 应具有正确的过期时间` | 会话管理 | ✅ 可以启用 |
| `会话超时后应自动登出` | 会话管理 | ⚠️ 需要配置超时时间 |

### 2. 注册模块 (register.spec.ts) - 8 个跳过

| 测试名称 | 跳过原因 | 建议 |
|---------|---------|------|
| `应该能够成功注册新用户` | 需要后端服务 | ✅ 可以启用 |
| `注册后应该能够使用新注册的账号重新登录` | 依赖注册成功 | ✅ 可以启用 |
| `应该能够输入邮箱验证码` | 功能可能不存在 | ❌ 保持跳过 |
| `验证码错误时应该显示错误消息` | 功能可能不存在 | ❌ 保持跳过 |
| `验证码过期后应该提示重新发送` | 功能可能不存在 | ❌ 保持跳过 |

### 3. Dashboard 模块 (dashboard.spec.ts) - 22 个跳过

| 测试名称 | 跳过原因 | 建议 |
|---------|---------|------|
| `应该能够创建空白图谱` | 核心功能 | ✅ 可以启用 |
| `搜索无结果时应该显示提示` | 基础功能 | ✅ 可以启用 |
| `应该能够删除指定图谱` | 核心功能 | ✅ 可以启用 |
| `应该能够取消删除操作` | 核心功能 | ✅ 可以启用 |
| `应该能够收藏图谱` | 功能存在 | ✅ 可以启用 |
| `应该能够取消收藏图谱` | 功能存在 | ✅ 可以启用 |
| `收藏状态应该在页面刷新后保持` | 功能存在 | ✅ 可以启用 |
| `收藏的图谱应该显示实心星星图标` | 功能存在 | ✅ 可以启用 |
| 收藏筛选相关测试 (5个) | 功能存在 | ✅ 可以启用 |
| 分享相关测试 (6个) | 功能可能不存在 | ❌ 保持跳过 |

### 4. 整个模块跳过

| 模块 | 文件 | 建议 |
|------|------|------|
| 任务调度器 | scheduler.spec.ts | ❌ 保持跳过（功能未完成） |
| 成就系统 | achievements.spec.ts | ❌ 保持跳过（功能未完成） |
| 集成测试 | integration.spec.ts | ❌ 保持跳过（功能未完成） |

## 实施计划

### 步骤 1: 启用登录模块测试
- 移除 5 个登录测试的 `test.skip`

### 步骤 2: 启用注册模块测试
- 移除 2 个核心注册测试的 `test.skip`

### 步骤 3: 启用 Dashboard 模块测试
- 移除 16 个核心 Dashboard 测试的 `test.skip`

### 步骤 4: 运行测试验证
- 运行 `npm run test:auth` 验证登录/注册测试
- 运行 `npm run test:dashboard` 验证 Dashboard 测试

## 预期结果

- 登录模块：+5 个测试
- 注册模块：+2 个测试
- Dashboard 模块：+16 个测试
- 总计：+23 个测试可启用

## 保持跳过的测试

- 邮箱验证相关测试（功能不存在）
- 分享功能相关测试（功能不存在）
- scheduler.spec.ts 整个文件
- achievements.spec.ts 整个文件
- integration.spec.ts 整个文件
