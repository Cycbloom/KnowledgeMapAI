## 修复分支节点显示问题

### 问题分析
数据库中已经有 `is_accepted` 字段，但后端代码没有处理这个字段，导致：
- 前端发送 `is_accepted` 字段
- 后端没有保存到数据库
- 所有节点都显示为圆形和实线

### 修复步骤

**修改 `api/routes/nodes.ts`**
1. 在解构 `req.body` 时包含 `is_accepted`
2. 在构建 `nodeData` 时包含 `is_accepted`

### 预期效果
- 修复后，未选择的分支节点会显示为方形和虚线
- 已选择的分支节点会显示为圆形和实线