# 嵌入向量逻辑优化计划

## 速度测试结果

### 单个标题生成嵌入向量

| 标题 | 耗时 |
|------|------|
| 机器学习基础 | 989ms |
| 深度学习概述 | 587ms |
| 神经网络原理 | 555ms |
| Python编程 | 521ms |
| 数据结构与算法 | 553ms |
| 监督学习 | 564ms |
| 卷积神经网络 | 497ms |
| 自然语言处理 | 560ms |
| 回归分析 | 560ms |
| 分类算法 | 700ms |

**平均耗时: ~600ms/个**

---

## 最终方案

| 问题 | 选择 |
|------|------|
| AI 生成时是否检查相似度？ | **并行检查 + 进度条** |
| 如何处理相似结果？ | **返回给前端选择** |
| 嵌入向量何时生成？ | **后台异步生成** |
| 并发数量 | **动态调整** |
| 进度条样式 | **进度条组件** |
| 相似选择 UI | **内嵌在列表中** |

---

## 实现方案

### 1. 后端：并行相似度检查

```typescript
// 动态并发控制
async function checkSimilarityParallel(titles: string[], concurrency: number = 5) {
  const results = [];
  for (let i = 0; i < titles.length; i += concurrency) {
    const batch = titles.slice(i, i + concurrency);
    const embeddings = await Promise.all(
      batch.map(title => aiService.generateEmbedding(title))
    );
    // 批量查询数据库...
  }
  return results;
}
```

### 2. 前端：进度条组件

```
┌─────────────────────────────────────────┐
│  正在检查知识点相似度...                  │
│  ████████████░░░░░░░░ 60%               │
│  已检查 6/10 个知识点                     │
└─────────────────────────────────────────┘
```

### 3. 前端：内嵌相似选择

```
┌─────────────────────────────────────────┐
│ ▼ 机器学习入门                           │
│   ⚠️ 发现相似知识点                       │
│   ○ 复用: 机器学习基础 (89%)              │
│   ● 创建新的                              │
├─────────────────────────────────────────┤
│ ▼ 深度学习简介                           │
│   ✓ 无相似知识点，将创建新的              │
├─────────────────────────────────────────┤
│ ▶ 神经网络原理                           │
│   正在检查...                            │
└─────────────────────────────────────────┘

[取消] [确认所有选择]
```

### 4. 后台异步生成嵌入

- 知识点创建时 `embedding = null`
- 后台任务定期扫描并补充

---

## 需要修改的文件

### 后端

| 文件 | 修改内容 |
|------|----------|
| `autoGraphService.ts` | 添加并行相似度检查函数 |
| `embeddingService.ts` | 优化并发控制 |
| `routes/autoGraph.ts` | 添加检查相似度 API |

### 前端

| 文件 | 修改内容 |
|------|----------|
| `AutoGraphGenerator.tsx` | 添加进度条和相似选择 UI |
| `api/autoGraph.ts` | 添加检查相似度 API 调用 |

---

## 实现步骤

1. **后端**: 添加并行相似度检查 API
2. **后端**: 优化嵌入服务并发控制
3. **前端**: 添加进度条组件
4. **前端**: 添加相似知识点选择 UI
5. **测试**: 端到端测试

---

## 预估工作量

- 后端修改: ~2 小时
- 前端修改: ~3 小时
- 测试: ~1 小时

确认后开始实现。
