您不需要再手动进行任何操作了。

**分析情况：**
检测到您的字体文件位于深层子目录中（例如 `api/assets/fonts/Inter,Noto_Sans_SC/Noto_Sans_SC/static/NotoSansSC-Regular.ttf`），而目前的后端代码仅会在 `api/assets/fonts` 的根目录下查找，这会导致找不到字体。

**我的行动计划：**
我将更新后端代码 (`api/routes/data.ts`)，使其具备**递归查找**功能。这样无论您将字体文件解压到了多深的子文件夹中，系统都能自动扫描并加载正确的 Noto Sans SC 中文字体，确保 PDF 导出功能完美运行。

**待办事项：**

1. 修改 `api/routes/data.ts`，增强字体扫描逻辑，支持递归遍历子目录。

