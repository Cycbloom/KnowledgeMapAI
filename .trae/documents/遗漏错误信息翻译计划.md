# 遗漏错误信息翻译计划

## 发现的问题
在 `api/routes` 目录下还有 16 处英文错误信息未翻译：

### 需要修改的文件

1. **api/routes/aiActions.ts** (5处)
   - `'Action not found'` → 使用 `ErrorCodes.ACTION_NOT_FOUND`
   - `'Not authorized to update this action'` → 使用 `ErrorCodes.NOT_AUTHORIZED`
   - `'Not authorized to delete this action'` → 使用 `ErrorCodes.NOT_AUTHORIZED`
   - `'action_id and node_id are required'` → 使用 `ErrorCodes.ACTION_ID_NODE_ID_REQUIRED`

2. **api/routes/prompts.ts** (3处)
   - `'Missing required fields'` → 使用 `ErrorCodes.MISSING_REQUIRED_FIELDS`
   - `'Cannot modify system templates directly'` → 使用 `ErrorCodes.CANNOT_MODIFY_SYSTEM_TEMPLATE`
   - `'Graph ID required for graph scope'` → 使用 `ErrorCodes.GRAPH_ID_REQUIRED`

3. **api/routes/periodicTasks.ts** (1处)
   - `'Missing taskType or value'` → 使用 `ErrorCodes.MISSING_TASK_TYPE_OR_VALUE`

4. **api/routes/ai/tts.ts** (1处)
   - `'TTS Provider not configured'` → 使用 `ErrorCodes.TTS_PROVIDER_NOT_CONFIGURED`

5. **api/routes/ai/document.ts** (2处)
   - `'No file uploaded'` → 使用 `ErrorCodes.NO_FILE_UPLOADED`
   - `'No image uploaded'` → 使用 `ErrorCodes.NO_IMAGE_UPLOADED`

6. **api/routes/ai/cards.ts** (3处)
   - `'Task not found'` → 使用 `ErrorCodes.TASK_NOT_FOUND`
   - `'Failed to fetch task'` → 使用 `ErrorCodes.FAILED_TO_FETCH_TASK`
   - `'Missing required fields: graph1_id...'` → 使用 `ErrorCodes.MISSING_GRAPH_NODES_FIELDS`

7. **api/routes/achievements.ts** (1处)
   - `'Missing type or value'` → 使用 `ErrorCodes.MISSING_TYPE_OR_VALUE`

## 执行步骤
1. 修改 aiActions.ts 使用错误码
2. 修改 prompts.ts 使用错误码
3. 修改 periodicTasks.ts 使用错误码
4. 修改 ai/tts.ts 使用错误码
5. 修改 ai/document.ts 使用错误码
6. 修改 ai/cards.ts 使用错误码
7. 修改 achievements.ts 使用错误码
8. 运行类型检查验证
