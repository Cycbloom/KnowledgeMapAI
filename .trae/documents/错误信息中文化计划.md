# 错误信息中文化计划

## 目标
将 API 路由中的英文错误信息全部改为中文，提升用户体验。

## 已完成的修改
1. ✅ `api/routes/auth.ts` - 登录接口错误信息中文化
2. ✅ `api/routes/data.ts` - 导出格式错误信息
3. ✅ `api/routes/prompts.ts` - 模板相关错误信息
4. ✅ `api/routes/periodicTasks.ts` - 周期任务错误信息
5. ✅ `api/routes/nodes.ts` - 节点相关错误信息

## 待修改的文件

### 1. `api/routes/knowledgePoints.ts`
需要修改的错误信息：
- "Permission denied" → "没有权限执行此操作"
- "Admin access required" → "需要管理员权限"

### 2. `api/routes/aiActions.ts`
需要修改的错误信息：
- "Graph ID required for graph scope" → "图谱级别操作需要提供图谱ID"
- "Graph not found" → "图谱不存在"
- "Not authorized to create action for this graph" → "没有权限为此图谱创建操作"
- "Action not found" → "操作不存在"
- "Not authorized to update this action" → "没有权限更新此操作"
- "Not authorized to delete this action" → "没有权限删除此操作"
- "action_id and node_id are required" → "缺少操作ID和节点ID"

### 3. `api/routes/ai/tts.ts`
需要修改的错误信息：
- "TTS Provider not configured" → "语音合成服务未配置"

### 4. `api/routes/ai/document.ts`
需要修改的错误信息：
- "No file uploaded" → "未上传文件"
- "No image uploaded" → "未上传图片"

### 5. `api/routes/ai/cards.ts`
需要修改的错误信息：
- "Task not found" → "任务不存在"
- "Failed to fetch task" → "获取任务失败"
- "Missing required fields: graph1_id, graph2_id, graph1_nodes, graph2_nodes" → "缺少必填字段：图谱ID和节点信息"

### 6. `api/routes/achievements.ts`
需要修改的错误信息：
- "Missing type or value" → "缺少类型或值"

## 执行步骤
1. 修改 `knowledgePoints.ts` 中的英文错误信息
2. 修改 `aiActions.ts` 中的英文错误信息
3. 修改 `ai/tts.ts` 中的英文错误信息
4. 修改 `ai/document.ts` 中的英文错误信息
5. 修改 `ai/cards.ts` 中的英文错误信息
6. 修改 `achievements.ts` 中的英文错误信息
7. 运行类型检查确保代码正确
