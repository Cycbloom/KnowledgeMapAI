## 现状扫描（从代码可见的“空缺/占位/断裂体验”）
1. **个人中心未实现**：`/profile` 直接渲染占位文案。见 [App.tsx:L21-L43](file:///d:/KnowledgeMap/src/App.tsx#L21-L43)。
2. **PDF 导出为占位实现**：后端 `GET /api/data/export/pdf` 返回固定字符串 `PDF CONTENT MOCK`。见 [data.ts:L11-L50](file:///d:/KnowledgeMap/api/routes/data.ts#L11-L50)。
3. **AI 未配置时大量走 Mock**：`/ai/*` 多个接口在无 Key 时返回“模拟内容/模拟图谱/模拟卡片/模拟聊天”，但前端缺少明确的“当前为模拟模式”提示与配置引导。见 [ai.ts:L41-L113](file:///d:/KnowledgeMap/api/routes/ai.ts#L41-L113)、[ai.ts:L312-L334](file:///d:/KnowledgeMap/api/routes/ai.ts#L312-L334)。
4. **学习卡片缺少“仅到期（due）”服务端过滤**：接口注释明确“先返回全部，未来可加 `?due=true`”，目前只能前端过滤，数据量变大时体验会差。见 [study.ts:L37-L98](file:///d:/KnowledgeMap/api/routes/study.ts#L37-L98)。
5. **后台任务有后端轮询处理但缺少前端闭环**：编辑器能提交 `generate_questions/expand_graph` 任务，但没有“任务中心/进度/完成提示/结果回填”。见 [GraphEditor.tsx:L618-L641](file:///d:/KnowledgeMap/src/pages/GraphEditor.tsx#L618-L641)、后端 [tasks.ts](file:///d:/KnowledgeMap/api/routes/tasks.ts) 与轮询处理 [server.ts:L12-L20](file:///d:/KnowledgeMap/api/server.ts#L12-L20)。
6. **智能连线推荐在 AI 未配置时静默返回空**：用户会误以为“没有推荐”，而非“功能不可用”。见 [ai.ts:L479-L486](file:///d:/KnowledgeMap/api/routes/ai.ts#L479-L486)。

## 最值得完成的功能（按投入产出比排序）
1. **任务中心（高价值 / 中等工作量）**
   - 目标：让“后台扩展图谱/后台生成题目”变成可追踪、可回填的产品级功能。
   - 方案：新增任务列表 UI（页面或抽屉/弹窗），用 TanStack Query 轮询 `GET /api/tasks`；任务完成时触发消息条动作（例如“查看结果/定位到新增节点/进入学习”）。
   - 关键改动点：前端消费 [api.ts:L199-L202](file:///d:/KnowledgeMap/src/services/api.ts#L199-L202)；补齐 UI 与状态展示。

2. **学习模式：到期卡（due）优先（高价值 / 低-中工作量）**
   - 目标：Study 默认只学习到期卡，支持切换“全部/到期”。
   - 方案：后端 `GET /study/cards` 支持 `?due=true`，用 `next_review <= now` 过滤；前端 `Study` 增加 toggle，并在图谱/节点入口默认进入 due。

3. **AI 配置状态提示与降级策略（高价值 / 低工作量）**
   - 目标：用户能明确知道当前 AI 是否可用，避免把 Mock 当真。
   - 方案：
     - 后端新增 `GET /api/ai/status`（返回 `{ enabled: boolean, provider: 'openai'|'deepseek'|null }`）或在现有 AI 响应里携带 `mock: true`。
     - 前端在 `GraphEditor / TextToGraphModal / ChatDialog` 入口处显示横幅或消息条：未配置时提示“当前为模拟模式/请配置环境变量”。
   - 同时优化：`/recommend-connections` 在未配置时返回 503 + 可读错误，而非空数组。

4. **个人中心（Profile）落地（中价值 / 低工作量）**
   - 目标：提供账号信息、退出、基础偏好（例如主题）、以及 AI 配置说明。
   - 方案：创建 `src/pages/Profile.tsx`，通过 `api.auth.getUser` 拉取展示（见 [api.ts:L45-L50](file:///d:/KnowledgeMap/src/services/api.ts#L45-L50)），并做基础资料更新（若后端缺少更新接口则补一个）。

5. **PDF 导出真正可用（中价值 / 中-高工作量）**
   - 目标：导出可分享的 PDF（至少包含图谱标题、节点层级、内容摘要）。
   - 方案：用 `pdfkit` 等库在后端生成 PDF（需要新增依赖）；或者前端将 Markdown 渲染后用浏览器打印为 PDF（零后端依赖但一致性略差）。
   - 现状：后端仍为 mock。见 [data.ts:L42-L47](file:///d:/KnowledgeMap/api/routes/data.ts#L42-L47)。

## 具体实施步骤（我建议按 1→2→3 的顺序落地）
### 1) 任务中心
- 前端：
  - 新增 `useTasksQuery`（轮询 `api.tasks.list`，例如 3-5s）。
  - 新增 UI：`/tasks` 页面或 `Layout` 右下角任务入口；列表展示 type/status/created_at/error。
  - 完成态回填：
    - `expand_graph` 完成后：提示“已新增 X 个节点”，点击后刷新图谱数据并高亮/定位新增节点。
    - `generate_questions` 完成后：提示“已生成题目”，点击跳转 `Study`。
- 后端：可选增强（若需要更强体验）：
  - `TaskService.updateTaskStatus` 写入更结构化的 `result`（例如新增 node_ids）。

### 2) Study：due 过滤
- 后端：`GET /study/cards` 支持 `due=true`；用 `next_review <= now` 过滤。
- 前端：`Study.tsx` 增加“到期/全部”切换，并默认到期。
- 验收：同一图谱卡片很多时，到期模式返回量显著减少，学习流畅。

### 3) AI 状态提示
- 后端：新增 `GET /ai/status` 或统一在 AI 响应里加 `mock` 字段。
- 前端：在 AI 入口处统一提示（MessageBar 或模块内 Banner），并对推荐连线的“未配置”给出明确错误。

### 4) Profile 页面
- 前端：新建 Profile 页面、接入用户信息展示；补充“配置 AI Key”的引导说明。
- 后端：如需编辑资料，新增 `PUT /auth/profile`（基于 `req.supabase` 更新 `public.users`）。

### 5) PDF 导出
- 选型 A（后端 pdfkit）：实现真实 PDF 流程，前端继续复用 `api.data.export` blob 下载。
- 选型 B（前端打印）：以 Markdown/HTML 为中间层，调用 `window.print`/打印样式导出。

## 验收标准（每项都可独立验收）
- 任务中心：能看到任务进度；完成后可一键进入结果（刷新图谱/进入学习）。
- due 学习：支持切换且默认到期；到期为空时有明确提示。
- AI 状态：未配置时所有 AI 入口都有一致提示；推荐连线不会“静默空”。
- Profile：不再是 Coming Soon；能展示当前用户信息并提供退出/说明。
- PDF：导出文件内容真实可读，不是 mock 字符串。

如果你同意，我会先落地 **任务中心 + due 学习 + AI 状态提示**（这三项最能明显提升产品闭环），然后再补 Profile 与 PDF。