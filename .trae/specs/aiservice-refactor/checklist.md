- [x] `aiUtils.ts` 已创建，包含 `isEnglishLanguage`、`dedupedRequest`、`generateRequestKey`、`pendingRequests`
- [x] `embeddingOps.ts` 已创建，包含 `generateEmbedding` 和 `generateEmbeddingsBatch`，导出 `embeddingOps` 单例
- [x] `chatService.ts` 已创建，包含 `chat` 和 `tutorChat`，tutorChat 的硬编码 prompt 已迁移，导出 `chatService` 单例
- [x] `cardGenerationService.ts` 已创建，包含 `generateCards` 及类型定义，导出 `cardGenerationService` 单例
- [x] `knowledgeExpansionService.ts` 已创建，包含 `expandKnowledge`、`getBranchSuggestions`、`suggestNextTopic`，suggestNextTopic 的硬编码 prompt 已迁移，导出 `knowledgeExpansionService` 单例
- [x] `contentGenerationService.ts` 已创建，包含 3 个内容生成方法及类型定义，导出 `contentGenerationService` 单例
- [x] `analysisService.ts` 已创建，包含 3 个分析提取方法，generateGraphFromImage 的硬编码 prompt 已迁移，导出 `analysisService` 单例
- [x] `AIService` 已改造为 Facade 门面，所有方法委托给子服务
- [x] `export const aiService = new AIService()` 导出保持不变
- [x] 所有 28 个调用方文件无需改动即可正常编译运行
- [x] `npm run check && npm run lint` 通过（check 零错误通过；lint 唯一错误为已有的 ActiveTaskPanel.tsx 问题）
