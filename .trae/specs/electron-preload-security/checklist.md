- [x] preload.ts 中 `ipc` 命名空间已移除，`window.electronAPI.ipc` 为 undefined
- [x] preload.ts 新增 `shell.openExternal(url)` 专用方法
- [x] 主进程 `shell:openExternal` 从 `ipcMain.on` 改为 `ipcMain.handle`，支持返回值
- [x] 主进程 `shell:openExternal` 拒绝非 http/https 协议的 URL
- [x] 主进程 IPC 通道白名单验证生效，非法通道调用被拒绝并记录日志
- [x] Login.tsx 使用 `electronAPI.shell.openExternal(url)` 替代 `electronAPI.ipc.send`
- [x] SetupWizard.tsx 使用 `electronAPI.shell.openExternal(url)` 替代 `electronAPI.ipc.send`
- [x] electronConfig.ts 使用 `electronAPI.api.getPort()` 替代 `electronAPI.ipc.invoke("api:getPort")`
- [ ] TypeScript 编译通过，无类型错误（存在 2 个预存类型错误于 backupService.ts 和 graphService.ts，与安全加固无关）
- [x] 渲染进程中无任何 `electronAPI.ipc` 残留引用
