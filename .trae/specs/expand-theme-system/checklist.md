- [x] ThemePreset 类型定义包含 7 个预设值（default/ocean/forest/sunset/lavender/rose/midnight）
- [x] themePresets.ts 配置文件包含每个预设的完整 50-900 色阶数据
- [x] ThemeProvider 扩展后同时管理 ThemeMode 和 ThemePreset，两者正交组合
- [x] ThemeProvider 将 themePreset class 正确应用到 `<html>` 元素
- [x] themePreset 选择持久化到 localStorage，应用启动时恢复
- [x] useTheme() 返回的 context 包含 themePreset、setThemePreset、availablePresets 新字段
- [x] useTheme() 保留原有 theme、themeMode、setTheme、toggleTheme、isDark 字段不变
- [x] CSS 变量为每个主题预设定义了亮色和暗色两套变体
- [x] Tailwind primary 色板改为引用 CSS 变量，随主题预设动态切换
- [x] Settings 页面显示主题预设选择器，每个预设以色块卡片展示
- [x] 主题预设切换后整个应用 UI 颜色立即更新
- [x] i18n 中英文翻译包含所有主题预设名称
- [x] 现有 ColorScheme（学习状态配色）系统未受影响
- [x] 运行 `npm run lint` 和 `npm run check` 无错误
