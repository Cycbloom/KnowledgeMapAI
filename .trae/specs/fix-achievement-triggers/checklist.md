- [x] AI 图谱保存节点后成就正确触发：通过 `/api/auto-graph/save-nodes` 保存节点后，`graphs_created` 和 `nodes_created` 成就被检查
- [x] `checkAndUnlockAchievements()` 覆盖所有 15 种 condition_type，无 `default: continue` 跳过
- [x] 路由层创建节点后发布 `node_created` 事件
- [x] 路由层创建边后发布 `edge_created` 事件
- [x] `AchievementSubscriber` 监听 `node_created` 事件并触发 `updateCreationStats()`
- [x] `AchievementSubscriber` 监听 `review_completed` 事件并触发 `updateMasteredStats()`
- [x] 专注会话结束时自动调用 `checkSpecialAchievements()` 检查夜猫子/早起鸟/周末战士成就
- [x] 完美主义者成就检查逻辑已实现：一天内完成所有计划任务时解锁
- [x] 多面手成就检查逻辑已实现：一天内完成5个不同类型任务时解锁
- [x] 周期性 streak 成就定时检查已配置：`checkPeriodicStreak()` 在定时任务中被调用
- [x] 路由层不再直接调用 `achievementService.updateCreationStats()` 等方法，改为事件驱动
- [x] `AchievementSubscriber` 的事件处理完整覆盖原路由层的所有成就触发逻辑（包括 `addXp` 调用）
- [x] `npm run check` 无类型错误
- [x] `npm run lint` 无代码规范问题
