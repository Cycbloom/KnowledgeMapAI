# Checklist

## 问题诊断

- [x] 骨干节点查询日志已添加，能够看到查询到的骨干节点数量和模块类型
- [x] 节点创建日志已添加，能够看到每个节点的 `parentId` 和 `targetModule`
- [x] 边创建日志已添加，能够看到要创建的边的数量和详细信息
- [x] 边创建验证日志已添加，能够看到实际创建的边数量

## 问题修复

- [x] 根据日志分析，确定了问题的根本原因
- [x] 修复了边创建逻辑（如果边创建失败）
- [x] 修复了骨干节点查询逻辑（如果查询失败）
- [x] 修复了 `parentId` 传递逻辑（如果传递失败）
- [x] 添加了必要的错误处理和降级逻辑

## 挂载状态反馈

- [x] API 返回了详细的挂载统计信息
- [x] API 返回了每个节点的挂载状态详情
- [x] 前端能够显示挂载状态（如果实现）

## 测试验证

- [x] 单元测试覆盖了边创建逻辑
- [x] 单元测试覆盖了骨干节点查询逻辑
- [x] E2E 测试覆盖了完整的文献提取流程
- [x] 手动测试验证了节点正确挂载到骨干节点下
- [x] 手动测试验证了前端正确显示层级关系

## 数据一致性

- [x] 节点的 `properties.backboneModule` 与目标模块一致
- [x] 存在对应的 `contains` 类型的边
- [x] 边的源节点为骨干节点
- [x] 前端 GraphOutline 正确显示父子关系
