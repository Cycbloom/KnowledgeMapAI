- [x] better-sqlite3 已安装且在 Electron 开发模式下可正常加载
- [x] SQLite Schema 适配层正确映射 PostgreSQL 特有类型（JSONB, vector, 枚举, TIMESTAMPTZ, UUID）
- [x] 所有核心表在 SQLite 中创建成功，包含 sync_status 和 local_updated_at 列
- [x] sync_metadata 和 sync_conflicts 表创建成功
- [x] DatabaseManager 的 CRUD 和批量操作方法可正常执行
- [x] JSONB 字段自动序列化/反序列化正确
- [x] IPC 通道 db:query, db:batch 可正常通信
- [x] preload.ts 暴露了 db 和 sync 命名空间
- [x] localClient 封装了 IPC 调用，API 签名与 request() 对齐
- [x] IPC 调用失败时自动降级到 HTTP API
- [x] adapter.ts 新增 electron-local 模式，优先使用 localClient
- [x] 云端专属操作（AI、向量搜索、RAG）始终走 HTTP 路径
- [x] 开发模式下不启用 IPC 路径，保持现有行为
- [x] POST /api/sync/pull 端点返回增量数据
- [x] POST /api/sync/push 端点批量应用变更并返回结果
- [x] Push 端点实现了 RLS 权限校验
- [x] Push 端点实现了冲突检测
- [x] SyncEngine Pull 同步正确合并云端变更到本地
- [x] SyncEngine Push 同步正确推送本地变更到云端
- [x] 冲突记录写入 sync_conflicts 表
- [x] 定时同步（30 秒间隔）正常工作
- [x] 首次全量同步成功拉取所有用户数据
- [x] 网络恢复时自动触发同步
- [x] SyncStatusIndicator 显示正确的同步状态图标
- [x] SyncDetailPanel 显示同步详情
- [x] SyncConflictPanel 支持冲突解决
- [x] 离线状态下可正常浏览和编辑图谱
- [x] 离线状态下可正常使用学习和任务功能
- [x] 离线状态下尝试云端功能显示友好提示
- [x] SQLite 初始化失败时降级到 HTTP 模式
