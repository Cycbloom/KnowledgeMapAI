- [x] batch_update_positions RPC 函数已改为 unnest 批量 UPDATE 实现
- [x] nodesService.batchUpdatePositions 使用 RPC 优先模式，失败回退到逐条更新
- [x] graphNodeService.batchUpdatePositions 同步应用 RPC 优先模式
- [x] create_edge RPC 函数已创建，合并验证+去重+插入/恢复逻辑
- [x] edgeService.create 使用 RPC 优先模式，降级路径行为一致
- [x] get_user_graph_tags RPC 函数已创建，数据库层完成标签提取+GROUP BY 计数
- [x] graphCrudService.getTags 使用 RPC 优先模式
- [x] get_graph_map_data RPC 函数已创建，数据库层完成图谱+计数+关系聚合
- [x] graphCrudService.getGraphMap 使用 RPC 优先模式
- [x] find_missing_connections RPC 函数已创建，数据库层完成 NOT EXISTS 排除已有连接
- [x] graphService.findMissingConnections 使用 RPC 优先模式
- [x] getResearchProgress 使用 SQL JSONB 条件过滤，不再全量加载后应用层过滤
- [x] batchUpdateNodes 降级路径改为 kp/gn 分开批量执行，不再逐条 HTTP 请求
- [x] 所有新增 RPC 函数在 16_grants.sql 中已授权
- [x] 所有 RPC 降级路径在 RPC 失败时行为与原逻辑一致
- [x] 类型检查通过（3 个已有错误与本次修改无关）
