- [x] 后端 `GET /graphs/:id/nodes?includeStatus=true` 返回 nodeStatus 字段，认证用户可获取学习状态
- [x] 后端 `GET /graphs/:id/nodes` 无 includeStatus 参数时行为与当前一致（向后兼容）
- [x] 后端 `POST /graphs/batch-node-status` 接受 graph_ids 数组，返回批量状态数据
- [x] 后端 `POST /graphs/batch-node-status` 有请求体校验（graph_ids 非空、上限 20）
- [x] 前端 IGraphsApi 接口包含 batchGetNodeStatus 方法签名
- [x] 前端 graphsApi.getNodes 支持 includeStatus 参数
- [x] 前端 graphsApi.batchGetNodeStatus 调用 POST /graphs/batch-node-status
- [x] 前端 useGraphData 返回 nodeStatus 字段（来自 includeStatus=true 请求）
- [x] 前端 useGraphNodeStatus staleTime 为 60 秒
- [x] 前端 useBatchGraphStatus 调用批量 API（单次请求）
- [x] GraphEditor.tsx 不再单独调用 useGraphNodeStatus
- [x] LearningMode.tsx 不再单独调用 useGraphNodeStatus
- [x] createOptimisticMutation 在 onSuccess 中用服务端响应更新缓存
- [x] 乐观 mutation 的 onSettled 不再发布 graph_data_changed 事件
- [x] FrontendEventSubscribers 的 graph_data_changed 订阅不再 invalidate graphData 查询键
- [x] 非乐观 mutation（useUpdateNodeMutation、useDeleteEdgeMutation 等）的事件发布行为不变
- [x] 移动端 graphsApi 适配 includeStatus 参数和 batchGetNodeStatus 方法
