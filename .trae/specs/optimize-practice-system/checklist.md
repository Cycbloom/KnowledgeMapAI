- [x] `study_cards.fsrs_state` 数据库字段类型为 TEXT，CHECK 约束包含 "New"/"Learning"/"Review"/"Relearning"
- [x] `studyService.dbCardToFSRS` 正确处理字符串 fsrs_state 到 FSRS State 枚举的映射
- [x] `reviewTaskService.createFirstReviewTask` 插入 fsrs_state 时使用字符串值
- [x] StudyCard 类型定义中 fsrs_state 字段类型为 string
- [x] `task_subtasks.mastery_level` 数据库注释标注为 "0.00-1.00"
- [x] 所有服务中 mastery_level 使用 0-1 量纲，无 0-100 残留
- [x] `reviewTaskService.updateReviewTask` 操作 `study_cards` 表并使用 FSRS 算法
- [x] `reviewTaskService.getPendingReviewTasks` 从 `study_cards` 查询到期卡片
- [x] `reviewTaskService.getReviewTaskStats` 基于 `study_cards` 计算 FSRS 统计
- [x] `subtaskQuizIntegration.completePractice` 完成后调用 `studyService.updateProgress` 更新 FSRS 调度
- [x] `subtaskQuizIntegration.completeQuiz` 完成后调用 `studyService.updateProgress` 更新 FSRS 调度
- [x] `updateCardReviewStats` 方法已重构，统一走 FSRS 路径
- [x] 前端 `studyApi.getCards` 只接受 `knowledge_point_id`/`knowledge_point_ids` 参数
- [x] 后端 `/study/cards` 路由只处理 `knowledge_point_id`/`knowledge_point_ids` 查询参数
- [x] AI 生成卡片入库前 `type` 字段已映射为 `card_type`
- [x] 代码中无 `card.type || card.card_type` 兼容写法
- [x] 任务复盘 API 导出命名为 `taskReviewApi`
- [x] 间隔重复复习 API 导出命名为 `studyReviewApi` 或 `spacedRepetitionApi`
- [x] 练习会话 API 路由（POST /study/practice-sessions, POST /study/practice-sessions/:id/complete）可用
- [x] 测验会话 API 路由（POST /study/quiz-sessions, POST /study/quiz-sessions/:id/complete）可用
- [x] 前端有对应的 practiceSession 和 quizSession API 客户端
- [x] `GET /study/stats` 端点返回基于 `study_cards` 的 FSRS 统计数据
- [x] 前端有对应的 FSRS 统计查询 hook
- [x] `sm2Service.ts` 所有导出有 `@deprecated` 标注和替代方案说明
- [x] `spacedRepetitionBridge.ts` 已移除 SM2 队列合并逻辑
- [x] `shared/types/scheduler.ts` 中 SM2 相关类型有明确的废弃注释
- [x] `ReviewTaskCard.tsx` 统一为 FSRS 模式显示
- [x] `npm run check` 类型检查通过
- [x] `npm run lint` 代码检查通过
