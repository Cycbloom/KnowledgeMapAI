- [x] TaskCard 使用 React.memo 包裹，自定义比较函数比较 task.id + task.status + task.updated_at
- [x] DraggableTaskCard 使用 React.memo 包裹，同上比较策略
- [x] Scheduler.tsx 中 handleStartTask/handlePauseTask/handleCompleteTask/handleDeleteTask/openEditTaskForm/handleViewTaskDetail 使用 useCallback
- [x] QueueColumn.tsx 中 .map() 内闭包回调改为稳定引用
- [x] HorizontalQueue.tsx 中 .map() 内闭包回调改为稳定引用
- [x] PlanetView 添加 enableRotation prop，默认 false，移除无条件 rotationAngleRef += 0.003
- [x] PlanetView useFrame 中引入 dirtyFlags，矩阵/颜色仅在标记 dirty 时更新
- [x] PlanetView 颜色更新逻辑分离为独立 useEffect，仅在 selectedNodeId/hoveredNodeId/coloringMode/nodeStatus 变化时执行
- [x] MindMapCanvas nodeImportanceMap 拆分为 nodesForImportance + nodeImportanceMap，依赖项从 7 降为 3+4
- [x] MindMapCanvas edgeStrengthMap 拆分为 linksForStrength + edgeLookupMap + edgeStrengthMap，依赖项从 6 降为 3+1+3
- [x] MindMapCanvas narrativeFilteredLinks 修复非叙事模式下 layoutLinks 无效依赖
- [x] MindMapCanvas 第 707 行 nodeMap 用 useMemo 包裹
- [x] analysis.ts 导出 calculateGlobalMaxDegree/calculateGlobalMaxChildren，calculateNodeImportance 接受参数而非内部重复计算
- [x] MindMapCanvas 添加 globalMaxDegree/globalMaxChildren useMemo 并传入 nodeImportanceMap
- [x] MindMapCanvas 组件外部定义 EMPTY_SET/EMPTY_MAP/EMPTY_ARRAY 常量，替换默认 prop 值
- [x] 运行 npm run check 确认无类型错误
- [x] 运行 npm run lint 确认无 lint 错误
