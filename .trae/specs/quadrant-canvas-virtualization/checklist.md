- [x] QuadrantCanvas 中 viewportVersion 状态和 rAF 节流逻辑已实现
- [x] 平移和缩放操作触发 scheduleViewportUpdate，视口更新被节流
- [x] useSpatialGrid 在节点数 > 100 时构建空间索引，<= 100 时返回 null
- [x] useViewportBounds 根据 transform 和容器尺寸计算视口边界
- [x] useVisibleNodes 仅返回视口内（含缓冲区）的节点
- [x] useVisibleEdges 仅返回 source 或 target 在视口内的边
- [x] 渲染循环仅渲染可见节点和边，离屏节点不进入 SVG DOM
- [x] RegionBackground 和 RegionHeader 始终渲染，不受虚拟化影响
- [x] 原点标记始终渲染
- [x] 缩放/平移交互体验与虚拟化前一致，无视觉闪烁
