- [x] 默认状态（无选中节点）下，边使用关系类型颜色渲染，透明度 ≥ 0.4，线宽 ≥ 1.2
- [x] 默认状态下，边保留关系类型的虚线样式（dashed/dotted），不被强制覆盖为实线
- [x] 聚焦模式下，高亮边使用关系类型颜色，opacity=1，strokeWidth=2.5，保留 dash 样式，有发光效果
- [x] 聚焦模式下，暗淡边使用关系类型颜色（非灰色），opacity=0.15，strokeWidth=1，保留 dash 样式，无发光效果
- [x] 聚焦模式下，非聚焦节点 opacity=0.45（非 0.3），仍可辨认标题和颜色
- [x] 选中节点后，只有通过 regionEdges 可见边与选中节点直接相连的邻居节点被高亮
- [x] 选中节点后，不存在"节点高亮但与选中节点间无可见连线"的幽灵高亮现象
- [x] 跨区域边（两端节点在不同可见区域）正常渲染和高亮
- [x] QuadrantEdge 组件正确接收并使用 `hasFocusMode` prop 区分三档渲染
- [x] 边端点缩短到节点圆圈边界，不穿过节点圆圈
- [x] visibleFocusedNodeIds 增加边可见性检查（距离 > 两节点半径之和才高亮）
- [x] 小图谱（≤5 节点）也应用碰撞避免
- [x] 类型检查通过（`npm run check`）
- [x] 代码检查通过（`npm run lint`）
