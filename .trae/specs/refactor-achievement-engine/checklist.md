- [x] `AchievementConditionEvaluator` 接口已定义，包含 `conditionType`、`getCurrentValue(userId)`、`relevantEvents`、`getIncrementalDelta(event)` 方法
- [x] Evaluator 注册表已实现，支持 `register()`、`get(conditionType)`、`getByEvent(eventType)` 查询
- [x] 15 种 condition_type 均有对应的 Evaluator 实现
- [x] 新增 condition_type 只需创建 Evaluator 类并注册，无需修改已有代码（开闭原则）
- [x] `achievements` 表已增加 `trigger_events TEXT[]` 字段
- [x] 种子数据中每个成就已配置 `trigger_events` 值
- [x] `evaluateAchievements(userId, eventType)` 方法只检查 trigger_events 包含该事件的成就
- [x] 事件到达时对 `user_achievements.progress` 做增量更新（progress += delta）
- [x] 每日校准任务全量重算所有成就 progress，修正增量误差
- [x] 成就解锁使用 upsert + onConflict 保证幂等，无竞态条件风险
- [x] `AchievementSubscriber` 所有事件处理器统一为调用 `evaluateAchievements(userId, eventType)`
- [x] `checkAndUnlock()` 和 `checkAndUnlockAchievements()` 两套旧方法已标记 @deprecated
- [x] `updateCreationStats()`、`updateMasteredStats()` 等直接调用方法已标记 @deprecated
- [x] 前端成就进度展示从 `progress/condition_value*100` 计算百分比
- [x] `npm run check` 无类型错误
- [x] `npm run lint` 无代码规范问题
