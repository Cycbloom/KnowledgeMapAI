# Checklist

## 同队列排序功能
- [x] 用户可以在同一队列内拖拽任务改变顺序
- [x] 拖拽时显示正确的视觉反馈（拖拽预览、占位符）
- [x] 释放后任务移动到新位置
- [x] 发送正确的 reorder API 请求
- [x] 显示成功消息 "任务顺序已更新"

## 跨队列移动功能
- [x] 用户可以将任务从一个队列拖拽到另一个队列
- [x] 拖拽经过目标队列时显示高亮效果
- [x] 释放后任务出现在目标队列中
- [x] 发送正确的 move API 请求
- [x] 显示成功消息 "任务已移动到 Q{X}"

## 拖拽到空队列
- [x] 用户可以将任务拖拽到没有其他任务的队列
- [x] 空队列正确接收拖放
- [x] 任务出现在空队列中

## 拖拽取消
- [x] 按 Escape 键可以取消拖拽
- [x] 拖拽到无效区域时任务返回原位置
- [x] 不发送任何 API 请求

## 视觉反馈
- [x] 拖拽开始时卡片显示半透明效果
- [x] 原位置显示占位符或虚线框
- [x] 拖拽预览跟随鼠标移动
- [x] 目标队列显示高亮边框
- [x] 同队列排序时显示插入位置指示器

## 代码质量
- [x] 类型检查通过 (npm run check) - 仅存在与本次修改无关的已存在错误
- [x] 无新增 lint 错误
- [x] 代码逻辑清晰，易于理解
- [x] 正确使用 dnd-kit 的 API
