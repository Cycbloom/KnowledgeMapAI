# Checklist

## 类型定义验证

- [x] `BackboneModule` 枚举类型正确定义，包含六个枚举值
- [x] `BACKBONE_MODULE_TITLES` 常量正确映射所有枚举值到中文标题
- [x] `BACKBONE_MODULE_ICONS` 常量正确映射所有枚举值到图标
- [x] `NodeProperties` 接口正确扩展，包含可选的 `backboneModule` 字段
- [x] 类型定义通过 TypeScript 编译检查

## 后端验证服务验证

- [x] `validateBackboneNodeTitle` 方法能正确识别标准标题
- [x] `validateBackboneNodeTitle` 方法能正确识别非标准标题
- [x] `correctBackboneNodeTitle` 方法能正确修正非标准标题
- [x] `validateBackboneModule` 方法能正确验证枚举值
- [x] `isBackboneNode` 方法能正确判断骨干节点
- [ ] 验证服务单元测试全部通过

## API 验证接口验证

- [x] `POST /api/graphs/:graphId/nodes/validate-backbone` 接口正常工作
- [x] 接口能正确返回验证结果
- [x] 接口能正确返回修正建议
- [x] 接口能正确返回错误信息
- [ ] API 集成测试全部通过

## AI 生成增强验证

- [x] 骨干网络生成 prompt 包含明确的标题要求
- [x] AI 生成的骨干节点标题符合标准
- [x] 验证服务能正确验证 AI 生成的标题
- [x] 不符合标准的标题能自动修正
- [x] 每个骨干节点包含正确的 `backboneModule` 属性
- [x] 生成日志正确记录标题修正情况
- [ ] 生成服务测试全部通过

## 模板生成验证

- [x] `template_type_topic_research` prompt 模板已更新
- [x] 模板生成使用标准标题
- [ ] 模板生成流程测试通过

## 数据库迁移验证

- [x] 迁移脚本能正确识别现有骨干节点
- [x] 迁移脚本能为现有骨干节点添加 `backboneModule` 属性
- [x] 迁移脚本不修改非骨干节点
- [ ] 迁移脚本测试通过

## 前端组件验证

- [x] `BackboneNodeIcon` 组件能正确渲染图标
- [x] 图标映射逻辑正确
- [x] 图标支持不同尺寸
- [x] tooltip 正确显示模块类型
- [ ] 组件测试全部通过

## 节点渲染验证

- [x] 骨干节点能正确显示专属图标
- [x] 图标位置正确（标题左侧）
- [x] 图标大小与节点大小成比例
- [x] 图标颜色与节点主题色一致
- [x] 普通节点不显示骨干图标
- [ ] 节点渲染测试通过

## 节点编辑验证

- [x] 骨干节点标题输入框显示为只读
- [x] 显示正确的提示信息
- [x] 其他属性（描述、标签等）仍可编辑
- [x] 普通节点标题可正常编辑
- [ ] 编辑限制功能测试通过

## 前端验证服务验证

- [x] `isBackboneNode` 方法正确判断骨干节点
- [x] `getBackboneModuleTitle` 方法正确返回标题
- [x] `getBackboneModuleIcon` 方法正确返回图标
- [ ] 服务测试全部通过

## API 保护机制验证

- [x] 尝试修改骨干节点标题时，请求被拒绝
- [x] 返回正确的错误信息
- [x] 修改其他属性时，请求正常处理
- [x] 修改普通节点标题时，请求正常处理
- [ ] API 保护机制测试通过

## 批量操作保护验证

- [x] 批量更新时，骨干节点标题修改被跳过
- [x] 返回正确的跳过提示
- [x] 其他字段正常更新
- [ ] 批量操作保护测试通过

## 兼容性处理验证

- [x] 能正确检测现有图谱骨干节点缺少属性
- [x] 能自动补充缺失的 `backboneModule` 属性
- [x] 能检测标题不符合标准
- [x] 提示用户是否标准化标题
- [x] 用户确认后正确执行标准化
- [ ] 兼容性处理流程测试通过

## 国际化验证

- [x] 中文翻译正确显示
- [x] 英文翻译正确显示
- [x] 错误提示翻译正确
- [ ] 国际化测试通过

## E2E 测试验证

- [x] 专题研究图谱初始化测试通过
- [x] 骨干节点标题标准化验证通过
- [x] 骨干节点图标显示测试通过
- [x] 骨干节点标题修改限制测试通过
- [x] API 保护机制测试通过
- [x] 现有图谱兼容性处理测试通过
- [x] 批量操作保护测试通过

## 用户体验验证

- [x] 骨干节点视觉标识清晰，易于区分
- [x] 编辑限制提示友好，易于理解
- [x] 错误提示清晰，用户知道如何处理
- [x] 兼容性处理流程顺畅，不影响用户使用
- [x] 国际化文本正确显示
