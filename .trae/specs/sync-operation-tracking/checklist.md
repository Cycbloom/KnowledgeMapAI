- [x] SQLite 性能 pragma 已添加（synchronous=NORMAL, cache_size, temp_store, mmap_size, busy_timeout）
- [x] pragma 设置通过读取确认生效
- [x] sync_operations 表在 schema.ts 中定义完整（含索引）
- [x] sync_operations 建表 SQL 在迁移文件中
- [x] DatabaseManager.create() 写入操作日志（action='create'）
- [x] DatabaseManager.update() 写入操作日志（action='update'，含 changed_fields）
- [x] DatabaseManager.delete() 先写操作日志再执行删除（action='delete'，含快照）
- [x] DatabaseManager.softDelete() 写入操作日志（action='delete'）
- [x] getPendingOperations() 方法可查询待同步操作
- [x] markOperationsSynced() 方法可标记操作已同步
- [x] cleanupSyncedOperations() 方法可清理旧操作日志
- [x] SyncEngine.pushToCloud() 基于操作日志构建请求，不再使用启发式猜测
- [x] 同一记录多次操作合并为最新一条
- [x] Push 成功后操作日志标记为 synced，原记录 sync_status 更新为 synced
- [x] Push 冲突时操作日志仍标记为 synced（云端优先策略不变）
- [x] 每次同步后自动清理 7 天前的已同步操作日志
- [x] 硬删除记录可通过操作日志正确同步到云端
