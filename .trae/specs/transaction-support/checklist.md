- [x] 事务执行器基础设施：`transactionExecutor.ts` 实现基于 pg 的 BEGIN/COMMIT/ROLLBACK 语义
- [x] 事务执行器：pg 不可用时降级为顺序执行并记录 warn 日志
- [x] `SupabaseAdapter.transaction()` 替换空壳实现，调用事务执行器
- [x] PostgreSQL 函数 `permanent_delete_graph` 实现图谱永久删除事务
- [x] PostgreSQL 函数 `soft_delete_graph_with_branches` 实现图谱软删除事务
- [x] PostgreSQL 函数 `batch_soft_delete_graphs` 实现批量图谱软删除事务
- [x] PostgreSQL 函数 `batch_permanent_delete_graphs` 实现批量图谱永久删除事务
- [x] `graphService.permanentDeleteGraph()` 使用 RPC 替代多步操作
- [x] `graphService.permanentDeleteGraphs()` 使用 RPC 替代多步操作
- [x] `graphService.deleteGraph()` 使用 RPC 替代多步操作
- [x] `graphService.deleteGraphs()` 使用 RPC 替代多步操作
- [x] PostgreSQL 函数 `create_knowledge_point_with_node` 实现节点原子创建
- [x] PostgreSQL 函数 `remove_node_with_edges` 实现节点+边原子软删除
- [x] PostgreSQL 函数 `batch_remove_nodes_with_edges` 实现批量节点+边原子软删除
- [ ] `nodes.ts` POST `/nodes` 使用 RPC 实现原子创建（已推迟：当前仍分步调用 knowledgePointService.create + graphNodeService.addToGraph，未使用 create_knowledge_point_with_node RPC）
- [x] `graphNodeService.removeFromGraph()` 使用 RPC 实现原子删除
- [x] `graphNodeService.batchDelete()` 使用 RPC 实现原子批量删除
- [x] `nodes.ts` POST `/nodes/batch-delete` 补充关联边删除逻辑（通过 graphNodeService.batchDelete 调用 batch_remove_nodes_with_edges RPC 实现）
- [ ] 数据导入 `/import` 和 `/import/markdown` 使用事务执行器（部分实现：导入了 transactionExecutor 但实际使用手动回滚而非事务执行器，仅 /reset 使用了事务执行器）
- [x] 数据重置 `/reset` 使用事务执行器
- [x] PostgreSQL 函数 `start_task_with_execution` 实现任务启动事务
- [x] PostgreSQL 函数 `complete_task_with_execution` 实现任务完成事务
- [x] `taskService.startTask()` 使用 RPC 实现原子操作
- [x] `taskService.completeTask()` 使用 RPC 实现原子操作
- [x] `taskService.reorderTasks()` 使用 RPC 或事务实现原子重排序
