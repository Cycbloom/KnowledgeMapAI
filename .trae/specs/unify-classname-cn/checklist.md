- [x] Button.tsx 使用 cn() 替代 .replace() 拼接 className
- [x] FormError.tsx 使用 cn() 替代 .replace() 拼接 className
- [x] FormField.tsx 使用 cn() 替代 .replace() 拼接 className
- [x] AudioVisualizer.tsx 使用 cn() 拼接动态 className
- [x] CodeBlock.tsx 使用 cn() 拼接动态 className
- [x] ConfirmationModal.tsx 使用 cn() 拼接动态 className
- [x] FocusTimer.tsx 使用 cn() 拼接动态 className
- [x] GlobalErrorBoundary.tsx 使用 cn() 拼接动态 className
- [x] GlobalSearch.tsx 使用 cn() 拼接动态 className
- [x] LazyImage.tsx 使用 cn() 拼接动态 className
- [x] MessageBar.tsx 使用 cn() 拼接动态 className
- [x] OfflineIndicator.tsx 使用 cn() 拼接动态 className
- [x] OfflineStatusBar.tsx 使用 cn() 拼接动态 className
- [x] PomodoroCycleBar.tsx 使用 cn() 拼接动态 className
- [x] ShortcutHelpPanel.tsx 使用 cn() 拼接动态 className
- [x] SSEStatusIndicator.tsx 使用 cn() 拼接动态 className
- [x] SyncStatusIndicator.tsx 使用 cn() 拼接动态 className
- [x] TagSystem.tsx 使用 cn() 拼接动态 className
- [x] VirtualList.tsx 使用 cn() 拼接动态 className
- [x] common 目录无 .replace(/\s+/g, ' ').trim() 用法
- [x] TypeScript 类型检查通过
