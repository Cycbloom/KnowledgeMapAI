- [x] FrontendEventBus 实现发布/订阅/取消订阅功能，单例导出
- [x] shared/types/events.ts 中新增 TimerEventType 及对应 Payload 类型
- [x] TimerService 作为单例管理所有计时器状态，内部使用 setInterval 驱动 tick
- [x] TimerService 所有状态变更通过 FrontendEventBus 发布事件
- [x] TimerService 实现核心方法：start、pause、resume、complete、skipToBreak、switchTask、setMode、reset
- [x] TimerService 计时结束时保存专注会话、播放通知音、发送浏览器通知
- [x] TimerService 从 useFocusStore 读取配置并响应配置变更
- [x] useUnifiedTimer hook 移除本地计时器逻辑，改为从 TimerService 订阅状态
- [x] useUnifiedTimer hook 返回值接口与原有一致
- [x] useUnifiedTimer hook 中操作方法委托给 TimerService
- [x] BreakTimer 移除独立 setInterval 倒计时逻辑
- [x] BreakTimer 使用 useUnifiedTimer hook 获取休息模式状态
- [x] BreakTimer 保留 UI 和休息建议轮播功能
- [x] useFocusStore 移除 tick() 方法
- [x] useFocusStore 移除 isActive、timeLeft、startTimer、pauseTimer、resetTimer、setDuration、setTaskId 等运行时状态
- [x] useFocusStore 保留 focusDuration、shortBreakDuration、longBreakDuration、soundEnabled、updateSettings 等配置项
- [x] FocusMode 通过重构后的 useUnifiedTimer 正常工作
- [x] FocusTimer 通过重构后的 useUnifiedTimer 正常工作
- [x] LearningFocusPanel 通过重构后的 useUnifiedTimer 正常工作
- [x] CurrentTask 通过重构后的 useUnifiedTimer 正常工作
- [x] MiniTimer 和 TaskTimer 作为纯展示组件正常工作
- [x] TimerService 监听 task_started 事件自动启动倒计时
- [x] timer_completed 事件可被调度器组件订阅
- [x] npm run lint 通过
- [x] npm run check 通过
- [x] 页面标题与计时器状态同步（运行时显示倒计时，停止时恢复默认）
