- [x] `useVirtualScroll` Hook 包含 `requestAnimationFrame` 节流逻辑
- [x] `useVirtualScroll` Hook 支持 `onEndReached` 回调，含去重逻辑
- [x] `useVirtualScroll` Hook 支持网格模式（双向滚动、列/行计算、坐标返回）
- [x] `useVirtualScroll` Hook 使用 `useMemo` 优化可见项计算
- [x] `useVirtualScroll` Hook 组件卸载时自动清理 rAF
- [x] `VirtualList` 组件内部使用 `useVirtualScroll` Hook，不再包含重复滚动逻辑
- [x] `VirtualList` Props 接口和导出保持不变
- [x] `VirtualGrid` 组件内部使用 `useVirtualScroll` Hook 网格模式，不再包含重复滚动逻辑
- [x] `VirtualGrid` Props 接口和导出保持不变
- [x] `npm run check` 类型检查通过
- [x] `npm run lint` 代码检查通过（仅预存问题，本次修改无新增）
