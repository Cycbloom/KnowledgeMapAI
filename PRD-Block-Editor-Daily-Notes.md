> **文档归属说明**：本文档为历史 PRD，P0 阶段实现见 `.trae/specs/add-notes-block-editor-daily/spec.md`。P1（AI 辅助/反向建图/AI 上下文/搜索/自定义模板/图片上传）与 P2（写作辅助/动态刷新/embedding/块引用）阶段尚未实现，本文档保留作为后续迭代的需求来源。

# 功能需求文档:块编辑器 + Daily Notes(学习反思空间)

| 项 | 内容 |
|----|------|
| 文档版本 | v1.0 |
| 创建日期 | 2026-07-03 |
| 文档状态 | 需求评审稿 |
| 编写角色 | 产品需求分析师 |
| 适用项目 | KnowledgeMap(个人学习中心) |
| 评审范围 | 功能需求,不含技术实现 |

---

## 1. 背景

### 1.1 现状

KnowledgeMap 已是一个功能高度集成的 AI 知识管理平台,围绕"知识图谱"构建了 **图谱编辑 → AI 加工 → FSRS 间隔重复 → 智能任务调度 → 游戏化激励** 的完整闭环。但当前所有知识沉淀在"图节点"中,缺少承载**长文本学习产出**(读书笔记、复盘、反思)的空间。

### 1.2 核心痛点

- **缺学习反思/笔记空间**:知识都在图节点里,读书笔记、复盘、长文输出无处安放。
- **复习与产出脱节**:FSRS 卡片是被动回忆,缺少"主动输出"。
- **碎片化输入入口缺失**(本次不纳入,记录在后续方向)。

### 1.3 本次目标

引入**块编辑器 + Daily Notes**,为个人学习场景补齐"反思/复盘/读书笔记"这一关键环节,形成 **图谱(结构化知识) ↔ 笔记(线性反思)** 的双向闭环。

### 1.4 非目标(本次不做)

- 块引用 / 块嵌入(块级双向链接)—— 留待后续迭代
- 数据库视图 / 查询块
- 团队协作、权限、发布
- 快捷捕捉(Quick Capture)、稍后读、剪藏
- 故事创作图谱(本次完全不动)
- 移动端速记入口

---

## 2. 核心场景与用户故事

### 2.1 场景一:每日学习反思(Daily Notes)

> 作为一个学习者,我每天学习完图谱节点、复习完卡片后,希望能在一个按日期组织的页面里记录今日所学、今日复习、今日反思,系统自动聚合今日数据,我不必手动统计。

**用户故事**:
- 当日首次打开"笔记"模块时,自动创建今天的 Daily Note(若不存在)
- Daily Note 顶部自动展示今日复习卡数、完成任务数、专注时长(只读聚合区)
- 正文按三段式模板:今日学习 / 今日复习 / 今日反思
- 我可以在正文中用 `[[节点名]]` 关联今天学到的图节点

### 2.2 场景二:读书笔记 / 主题笔记

> 作为一个学习者,我在读一本书或学一个主题时,希望用块编辑器撰写结构化笔记,并能把笔记挂载到对应的图谱节点,让笔记内容参与图谱的 AI 检索与问答上下文。

**用户故事**:
- 新建一篇普通笔记,用块编辑器撰写(标题、列表、引用、代码块等)
- 在笔记中写 `[[某个节点]]`,该节点详情页自动出现这篇笔记(挂载即链接)
- 当我在图谱里向 AI 提问时,挂载在该节点的笔记内容会作为上下文参与回答

### 2.3 场景三:AI 辅助反思与反向建图

> 作为一个学习者,我写完一段反思后,希望 AI 帮我总结要点,或从笔记中提取知识点反向补充到图谱,让"线性笔记"反哺"结构化图谱"。

**用户故事**:
- 在 Daily Note 中一键生成"当日学习总结"(基于今日复习卡片 + 完成任务)
- 在任意笔记中调用"提取要点反向建图",AI 从笔记抽取知识点,生成新节点 / 补充到现有节点
- 选中一段文字,调用"写作辅助"(续写 / 改写 / 扩写)

---

## 3. 关键设计决策(已对齐)

| # | 决策点 | 选定方案 | 备选(已排除) |
|---|--------|---------|---------------|
| D1 | 笔记实体与图谱关系 | **混合:独立实体 + 可挂载节点** | 独立+wiki链接;节点即笔记 |
| D2 | Daily Notes 生成方式 | **自动创建 + 模板化** | 纯手动;自动+聚合无模板 |
| D3 | 块编辑器深度 | **基础块编辑器**(不做块引用) | 加块引用/嵌入;加查询块 |
| D4 | 存储格式 | **Markdown 文本** | JSON blocks;MD+块ID混合 |
| D5 | 挂载交互 | **wiki 链接即挂载**(`[[节点名]]` 同时完成链接+挂载) | 引用与挂载分离;节点侧主动添加 |
| D6 | Daily 默认模板 | **三段式**:今日学习 / 今日复习 / 今日反思 | 五段式;极简自由区 |
| D7 | AI 辅助范围 | **总结 + 反向建图 + 写作辅助** | 仅总结;总结+反向建图 |

### 3.1 存储格式选型说明

选择 Markdown 的理由:
1. D3 选定基础块编辑器,不做块引用,块 ID 强需求消失
2. Markdown 兼容性强,可外部编辑、可导入导出,符合个人学习"数据自主"
3. 同步/冲突处理简单(文本 diff 优于 JSON 树 diff),契合现有 P2P 同步与本地 SQLite
4. 与现有架构一致(节点内容、AI 生成内容已为 Markdown 风格)
5. 块编辑器 UI 与 Markdown 存储解耦:界面呈现为块,落盘为 Markdown(类 Obsidian/Typora)
6. 未来升级块引用可叠加块 ID 标记(Obsidian `^block-id` 语法),向前兼容

---

## 4. 功能范围

### 4.1 本次纳入

| 模块 | 内容 |
|------|------|
| M1 笔记实体与数据模型 | notes / note_node_links 表,笔记 CRUD |
| M2 块编辑器 | 基础块类型 + 斜杠命令 + Markdown 快捷输入 + wiki 链接 |
| M3 Daily Notes | 自动创建 + 模板系统 + 三段式默认模板 + 自动聚合区 |
| M4 笔记管理 | 列表页、视图分类、标签、搜索 |
| M5 与图谱联动 | wiki 链接即挂载、节点详情页关联笔记、AI 上下文参与 |
| M6 AI 辅助 | 当日总结、反向建图、写作辅助 |

### 4.2 本次不纳入(后续迭代)

- 块引用 / 块嵌入
- 数据库视图 / 查询块
- 快捷捕捉、稍后读、剪藏
- 移动端速记入口
- 故事创作图谱(本次完全不动)

---

## 5. 详细功能需求

### M1 · 笔记实体与数据模型

#### M1.1 数据模型(概念级)

**notes 表**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| user_id | uuid | 所属用户(RLS) |
| title | text | 标题 |
| content | text | Markdown 正文 |
| type | enum | `note` / `daily` |
| date | date | 仅 daily 使用(每日一条) |
| template_id | uuid | 生成时所用模板(daily) |
| tags | text[] | 标签数组 |
| folder_id | uuid | 文件夹(可选,null=根) |
| is_pinned | bool | 是否置顶 |
| is_archived | bool | 是否归档 |
| created_at | timestamptz | |
| updated_at | timestamptz | |
| deleted_at | timestamptz | 软删除(对齐回收站机制) |

**约束**:`type='daily'` 时 `(user_id, date)` 唯一。

**note_node_links 表**(挂载关系,多对多)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| note_id | uuid | 笔记 |
| node_id | uuid | 图节点 |
| graph_id | uuid | 所属图谱(冗余,便于按图谱查询) |
| created_at | timestamptz | |

**约束**:同一 (note_id, node_id) 唯一。

**note_templates 表**(Daily 模板)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| user_id | uuid | null=系统默认模板 |
| name | text | 模板名 |
| content | text | 模板 Markdown(含变量占位) |
| is_default | bool | 是否为用户默认模板 |
| created_at | timestamptz | |

#### M1.2 功能需求

- **FR-M1-1**:支持笔记 CRUD(创建、查询、更新、软删除)
- **FR-M1-2**:支持笔记挂载关系 CRUD(增删查,不支持改)
- **FR-M1-3**:删除笔记时,级联清理 note_node_links
- **FR-M1-4**:笔记支持软删除,进入现有回收站(RecycleBin)
- **FR-M1-5**:RLS 行级安全,user_id 隔离

**验收标准**:
- 创建笔记 → 返回完整笔记对象
- 软删除笔记 → note_node_links 同步清理,笔记进入回收站可恢复(恢复时挂载关系不自动恢复,需提示)
- 跨用户访问笔记 → 403

---

### M2 · 块编辑器

#### M2.1 支持的块类型

| 块类型 | Markdown 语法 | 说明 |
|--------|--------------|------|
| 段落 | 纯文本 | 默认块 |
| 标题 | `#` `##` `###` | H1-H3 |
| 无序列表 | `-` 或 `*` | 支持嵌套缩进 |
| 有序列表 | `1.` | |
| 待办列表 | `- [ ]` / `- [x]` | 可勾选 |
| 引用 | `>` | |
| 代码块 | ```` ``` ```` | 带语言标注 |
| 分割线 | `---` | |
| 图片 | `![](url)` | 支持上传与外链 |
| 表格 | `\| ... \|` | GFM 表格 |
| wiki 链接 | `[[节点名]]` | 关联图节点(D5) |

#### M2.2 功能需求

- **FR-M2-1**:斜杠命令 `/` 在空行唤起块菜单,可选择块类型插入
- **FR-M2-2**:Markdown 快捷输入——行首输入 `#`/`-`/`>`/```` ``` ```` 等,自动转换为对应块
- **FR-M2-3**:块拖拽排序(上下拖动调整顺序)
- **FR-M2-4**:`[[` 触发图节点自动补全(按图谱 / 节点名模糊匹配),选中后插入 `[[节点名]]` 并创建挂载关系(D5)
- **FR-M2-5**:wiki 链接渲染为可点击,点击跳转到对应图谱节点
- **FR-M2-6**:支持图片上传(本地存储或对象存储,复用现有图片能力)
- **FR-M2-7**:自动保存(防丢失),编辑器失焦或定时(如 3 秒)落盘
- **FR-M2-8**:支持撤销/重做(Ctrl+Z / Ctrl+Shift+Z)
- **FR-M2-9**:兼容暗色模式(项目已有暗色主题)

**验收标准**:
- 输入 `/` → 弹出块菜单,可选 10 种块类型
- 输入 `[[` → 弹出节点补全,选择后正文插入链接 + 后台创建 note_node_links
- 点击已存在的 `[[节点名]]` → 跳转到该节点
- 拖拽块 → 顺序更新,Markdown 落盘正确

#### M2.3 可复用现有资产

- `shared/utils/wikiLink.ts` —— wiki 链接解析(D1 已有,可复用)
- `src/utils/wikiLinkRemarkPlugin.tsx` —— Markdown remark 插件(已有,可复用)
- `shared/utils/markdownParser.ts` / `src/utils/markdownParser.ts` —— Markdown 解析(已有)

---

### M3 · Daily Notes

#### M3.1 自动创建

- **FR-M3-1**:当日首次进入"笔记"模块时,若当天 daily note 不存在,自动创建
- **FR-M3-2**:自动创建使用用户的默认模板(无自定义模板则用系统默认三段式模板)
- **FR-M3-3**:自动创建后跳转到该 daily note 编辑器

#### M3.2 模板系统

- **FR-M3-4**:系统默认模板(不可删),结构为三段式:

  ```markdown
  # {{date}} 学习日志

  ## 今日数据
  - 复习卡片:{{today_reviewed_cards}}
  - 完成任务:{{today_completed_tasks}}
  - 专注时长:{{today_focus_time}}

  ## 今日学习

  ## 今日复习

  ## 今日反思
  ```

- **FR-M3-5**:用户可创建/编辑/删除自定义模板(系统默认模板不可删)
- **FR-M3-6**:用户可指定一个模板为"默认模板"(同时只能一个)
- **FR-M3-7**:模板支持变量占位,创建 daily note 时替换

#### M3.3 支持的模板变量

| 变量 | 含义 | 数据来源 |
|------|------|---------|
| `{{date}}` | 当日日期(本地化) | 系统时间 |
| `{{today_reviewed_cards}}` | 今日复习卡数 | study_progress |
| `{{today_completed_tasks}}` | 今日完成任务数 | task_executions |
| `{{today_focus_time}}` | 今日专注时长(分钟) | focus_sessions |

> 注:聚合数据在模板渲染时写入正文为**静态值快照**(便于历史追溯),后续用户编辑不影响。是否提供"动态刷新"按钮,列为 P2。

**验收标准**:
- 7月3日首次进入笔记模块 → 自动创建 7月3日 daily note,标题"2026-07-03 学习日志"
- 正文顶部"今日数据"区已填入今日复习/任务/专注统计
- 三段式空标题已就位,等待用户填写
- 切换默认模板后,次日新建 daily note 使用新模板

---

### M4 · 笔记管理

#### M4.1 笔记列表页

- **FR-M4-1**:新增路由 `/notes`,作为笔记模块入口(在主导航注册)
- **FR-M4-2**:列表按 `updated_at` 倒序,置顶笔记(is_pinned)优先
- **FR-M4-3**:列表项显示:标题、类型徽章(note/daily)、更新时间、标签、挂载节点数
- **FR-M4-4**:支持视图切换:全部 / Daily / 普通 / 收藏 / 归档

#### M4.2 操作

- **FR-M4-5**:新建笔记(普通)、新建 Daily(手动触发今日)
- **FR-M4-6**:置顶 / 取消置顶
- **FR-M4-7**:归档 / 取消归档(归档后不出现在"全部"视图)
- **FR-M4-8**:删除(软删除,进回收站)
- **FR-M4-9**:标签管理(增删改查,复用现有 TagSystem 组件)

#### M4.3 搜索

- **FR-M4-10**:笔记内容纳入全局搜索(复用现有 search 服务,扩展数据源)
- **FR-M4-11**:列表页支持按标题/标签本地筛选

**验收标准**:
- `/notes` 入口在主导航可见
- 切换视图 → 列表正确过滤
- 全局搜索"某关键词" → 命中的笔记出现在结果中

---

### M5 · 与图谱联动

#### M5.1 wiki 链接即挂载(D5)

- **FR-M5-1**:笔记中 `[[节点名]]` 保存时,自动创建 note_node_links 记录(去重)
- **FR-M5-2**:笔记中删除 `[[节点名]]` 保存时,自动删除对应 note_node_links
- **FR-M5-3**:节点重命名时,同步更新引用该节点的笔记正文(类现有 backlinks 机制)

#### M5.2 节点详情页关联笔记

- **FR-M5-4**:节点详情侧边栏新增"关联笔记"区块,列出挂载到该节点的笔记
- **FR-M5-5**:点击关联笔记 → 打开笔记编辑器(只读或编辑,根据权限)
- **FR-M5-6**:复用现有 BacklinksPanel 组件模式,新建 NotesPanel

#### M5.3 AI 上下文参与

- **FR-M5-7**:图谱 AI 问答 / RAG 检索时,挂载在当前节点(及相关节点)的笔记内容作为上下文参与
- **FR-M5-8**:笔记内容建立 embedding,纳入语义检索数据源

**验收标准**:
- 笔记 A 写了 `[[节点X]]` → 节点X详情页"关联笔记"出现笔记A
- 删除笔记A中的 `[[节点X]]` → 关联关系消失
- 图谱中向 AI 提问节点X相关问题 → 回答能引用笔记A的内容

---

### M6 · AI 辅助

#### M6.1 当日学习总结

- **FR-M6-1**:Daily Note 工具栏提供"生成今日总结"按钮
- **FR-M6-2**:AI 基于今日复习卡片(内容+掌握度)、完成任务、专注数据,生成一段结构化总结
- **FR-M6-3**:总结插入到"今日反思"段(或用户指定位置),可二次编辑
- **FR-M6-4**:复用现有 AI 对话与 Prompt 三层管理机制(System < User < Graph)

#### M6.2 笔记提取要点反向建图

- **FR-M6-5**:任意笔记工具栏提供"提取要点建图"按钮
- **FR-M6-6**:AI 从笔记正文提取知识点,返回候选节点列表(含建议关系)
- **FR-M6-7**:用户确认后,将选中知识点创建为新节点 / 合并到现有节点(可选目标图谱)
- **FR-M6-8**:建图完成后,自动在新节点与本笔记间建立 note_node_links

#### M6.3 写作辅助

- **FR-M6-9**:选中文本,右键/工具栏提供"续写 / 改写 / 扩写"
- **FR-M6-10**:AI 返回结果以 diff / 建议形式呈现,用户确认后替换或追加
- **FR-M6-11**:写作辅助复用现有 AI Provider 与性能监控

**验收标准**:
- Daily Note 点"生成今日总结" → "今日反思"段插入一段总结文本
- 普通笔记点"提取要点建图" → 弹出候选知识点,确认后图谱新增节点且笔记挂载到新节点
- 选中文字点"续写" → 文本以建议形式追加,确认后落盘

---

## 6. 优先级评估

采用 **MoSCoW + 分期**:

### P0(MVP,必须完成)

| 编号 | 功能 | 理由 |
|------|------|------|
| M1 | 笔记实体与数据模型 | 一切基础 |
| M2.1-M2.5 | 块编辑器核心(块类型 + 斜杠命令 + Markdown 输入 + wiki 链接) | 核心体验 |
| M2.7-M2.8 | 自动保存 + 撤销重做 | 不可缺失 |
| M3.1-M3.4 | Daily Notes 自动创建 + 系统默认三段式模板 | 核心场景 |
| M4.1-M4.2 | 笔记列表页 + 基础操作 | 入口 |
| M5.1-M5.2 | wiki 链接即挂载 + 节点详情关联笔记 | 闭环关键 |

### P1(高价值,本期完成)

| 编号 | 功能 | 理由 |
|------|------|------|
| M6.1 | 当日学习总结 | Daily 价值放大 |
| M6.2 | 笔记提取要点反向建图 | 线性→结构闭环 |
| M5.3 | AI 上下文参与 | 图谱←笔记闭环 |
| M4.10-M4.11 | 笔记搜索 | 可查找 |
| M3.5-M3.6 | 自定义模板 | 个性化 |
| M2.6 | 图片上传 | 笔记完整性 |

### P2(增强,后续迭代)

| 编号 | 功能 | 理由 |
|------|------|------|
| M6.3 | 写作辅助(续写/改写/扩写) | 体验增强 |
| M3.7 | 模板"动态刷新"聚合数据按钮 | 数据时效 |
| M5.3 FR-M5-8 | 笔记 embedding 纳入语义检索 | 检索增强 |
| M2 块引用 / 块嵌入 | | 下一阶段核心 |
| 快捷捕捉 / 稍后读 | | 碎片化输入 |

---

## 7. 技术依赖与可复用资产

### 7.1 可复用现有资产(降低成本)

| 资产 | 路径 | 用途 |
|------|------|------|
| wiki 链接解析 | `shared/utils/wikiLink.ts` | M2 wiki 链接解析 |
| Markdown remark 插件 | `src/utils/wikiLinkRemarkPlugin.tsx` | M2 渲染 |
| Markdown 解析器 | `shared/utils/markdownParser.ts` 等 | M2 渲染 |
| 反向链接 | `api/routes/backlinks.ts` + `shared/types/backlink.ts` | M5 关联笔记面板 |
| 回收站 | `RecycleBin` 页面 + 软删除机制 | M1.4 软删除 |
| TagSystem | `src/components/common/TagSystem.tsx` | M4.9 标签 |
| AI 对话/Prompt 三层管理 | `api/services/ai/` | M6 AI 辅助 |
| 全局搜索 | `api/routes/search.ts` | M4.10 笔记搜索 |
| Kernel 插件注册 | `src/services/kernel/plugins.ts` | M4.1 路由/导航注册 |

### 7.2 新增依赖

- 块编辑器 UI 组件(自研或选型,需评估):块拖拽、斜杠命令、块菜单
- note_node_links 的反向链接维护逻辑(参考现有 backlinks 机制扩展)

### 7.3 数据库变更

- 新增 `notes` / `note_node_links` / `note_templates` 三张表
- 修改现有 `search` 服务,纳入 notes 数据源
- 修改图谱 AI 上下文构建,纳入挂载笔记内容
- schema 变更后需运行 `npm run db:gen-types` 重新生成类型

---

## 8. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| wiki 链接即挂载导致"误挂载"(用户只是想引用) | 体验 | 挂载关系可视化(节点详情页可见),且可手动移除关联 |
| 块编辑器选型与现有 Markdown 解析栈冲突 | 开发 | 优先复用现有 markdownParser + remark 插件栈 |
| 自动聚合数据与 FSRS / scheduler 数据耦合 | 维护 | 聚合查询独立服务,只读,避免双向耦合 |
| 节点重命名导致笔记 wiki 链接失效 | 数据 | 复用现有 backlinks 重命名同步机制 |
| 笔记内容参与 RAG 后 embedding 成本上升 | 成本 | 笔记 embedding 走增量,仅更新变更部分 |
| Daily Note 自动创建与多端同步冲突 | 同步 | `(user_id, date)` 唯一约束 + 同步冲突解决(复用现有机制) |

---

## 9. 后续迭代方向(记录,不在本次范围)

1. **块引用 / 块嵌入**:升级到块级双向链接(叠加 `^block-id`),动态聚合页
2. **快捷捕捉 + 稍后读**:Quick Capture、网页/截图剪藏、阅读清单(对标 Readwise / Obsidian Web Clipper)
3. **命令面板 Cmd+K**:全局快速跳转
4. **移动端速记入口**:碎片化输入
5. **复习后主动输出**:费曼学习法、复习卡片后用自己话重写
6. **长期目标 + 周月复盘**:目标管理、复盘机制
7. **故事创作图谱**:评估是否剥离(本次完全不动)

---

## 10. 评审检查清单

- [ ] 核心场景与用户故事是否覆盖预期
- [ ] 7 个关键设计决策是否最终确认
- [ ] M1-M6 功能需求是否完整、无遗漏
- [ ] P0/P1/P2 优先级划分是否合理
- [ ] 可复用资产清单是否准确
- [ ] 风险对策是否可接受
- [ ] 后续迭代方向是否记录完整

---

**文档结束**
